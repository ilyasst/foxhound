# Overlay: Project Alpha

This optional overlay narrows an agent to one fictional project.

Only the `project-alpha` repository is in scope. Treat every other repository
of Example Org as out of scope and say so instead of reading it.
